#pragma once

#include <vector>

// Returns the duplicated number in an array containing n + 1 integers
// where each integer is in the range [1, n], inclusive.
int findDuplicate(std::vector<int> &nums);
