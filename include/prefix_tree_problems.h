#pragma once

#include <string>
#include <vector>

std::vector<std::string> findWords(const std::vector<std::vector<char>> &board,
                                   std::vector<std::string> &words);
